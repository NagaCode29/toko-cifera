class OrderDetail{
    constructor(order_id, product_id, quantity, price) {
        this.order_id = order_id;
        this.product_id = product_id;
        this.quantity = quantity;
        this.price = price;
    }
}
export default OrderDetail;